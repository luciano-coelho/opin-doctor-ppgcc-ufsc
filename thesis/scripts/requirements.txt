# Pinned to the exact versions validated for the pqc CRYPTO_PROFILE (see
# README.md's "Virtual environment" section for why this is needed at
# all -- in short, presenting an ML-DSA-65 client certificate requires
# routing TLS through cryptography's bundled OpenSSL via pyOpenSSL, not
# through the system/Python-bundled OpenSSL requests uses by default).
requests==2.34.2
PyJWT==2.13.0
cryptography==50.0.0
pyOpenSSL==26.4.0
urllib3==2.7.0
